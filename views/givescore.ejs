<!DOCTYPE html>
<html>
  <head>
    <title>give score</title>
    <%- include layout/link_head %>
    
  </head>
  <body>
    <!-- <%= JSON.stringify(query) %> -->
    <!-- <p>============</p> -->
    <%- include layout/header %>    
    
    <h2 align="center"><%= query.Subject.subject_name %></H2>
    <h5 align="center"><button class="btn btn-success" id="hyper" type="button" onclick="location.href='/subjects'">Return to Subject List</button></h5>
    <p></p>
    <table class="table table-hover">
      <tr>
        <th>Name</th>
        <th>Score</th>
      </tr>
      <tr>
        <td><%= query.Student.first_name + ' ' + query.Student.last_name %></td>    
        <td>
          <form action="/subjects/<%= query.SubjectId %>/givescore/<%= query.StudentId %>" method="post">
            <input type="text" name="score" value="" placeholder="0 - 100">
            <button type="submit" class="btn btn-danger">Add Score &#9745</button>
          </form>
        </td>
      </tr>
      
    </table>
  </body>
</html>